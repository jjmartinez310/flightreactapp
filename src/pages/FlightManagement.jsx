import { FlightTiles } from "../components/FlightTiles";
export const FlightManagement = () => {
    return(
        //loads the flight tiles onto the management page
        <>
            <FlightTiles />
        </>
    )
}